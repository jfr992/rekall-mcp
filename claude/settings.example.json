{
  "_comment": "Copy the hooks block into ~/.claude/settings.json. Replace ~ with /Users/<you>/ — JSON does not expand the tilde. If you already have a settings.json, MERGE entries into the matching event arrays rather than overwriting.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/memento-restore.sh"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/hooks/memento-observe.sh"
          }
        ]
      }
    ]
  }
}
