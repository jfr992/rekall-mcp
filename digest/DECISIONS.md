# Decision Log (ADR Shadow)

_47 entries · auto-generated from memory_

## byte-edge (47)

- BE-563 decision (2026-06-01): CRD sync from byte-secrets-operator to byte-edge/helm will use a PUSH model — BSO repo CI, on semantic-release tag, auto-opens an MR to byte-edge/helm regenerating charts/byte-secrets-operator/templates/crds/. Chose this over relocating the chart into the BSO repo (true ESO monorepo model) because of a hard integration wall: byte-edge-cli's EnsureRegistryMirrored calls FindRegistryByEndpoint(pack.yaml helmRepoFetchEndpoint), which is keyed to byte-edge/helm's per-repo GitLab package registry (projects/${CI_PROJECT_ID}/packages/helm). Moving the chart to the BSO repo changes CI_PROJECT_ID, publishes to a different registry Spectro doesn't mirror, and breaks publish until a new Spectro registry mirror is manually stood up + full-pipeline ported into BSO CI. Push model keeps registry/pipeline/byte-edge-cli untouched while still removing the manual-sync drift. Supersedes the pull/drift-check approach in MR !143 (sync-crds.sh + crd-drift-check CI job) — that work is largely throwaway; MR !143 needs to be closed or scoped down.  _(2026-06-01)_
- Diagnostic order-of-operations for "byte-edge cluster won't stabilize" sessions (codified from 2026-05-28 kfc-uk-lab recovery, ~3h of incremental fixes). Work through these checks in order — fixing earlier ones unblocks later ones: (1) Are there stale APIServices (`get apiservice | grep False`)? — they break namespace deletion cluster-wide. (2) Are there orphaned admission webhooks (`get validatingwebhookconfiguration,mutatingwebhookconfiguration` for any operator whose service is missing)? — they fail-closed on PVC/CR mutations. (3) Is the `namespace-labeler-labels` configmap free of unrendered Spectro templates? (4) Are there squatting old Helm releases in `default` ns or chart-named namespaces from prior pack.namespace migrations? `helm uninstall` them. (5) Are there ghost `Unknown` pods? `--force --grace-period=0`. (6) Is `byte-edge-secrets-manager` ClusterSecretStore present and Valid? If not, `helm get manifest byte-edge-common | kubectl apply -f -` to recreate the CSS + ExternalSecrets. (7) Are MongoDB phase=Running (not Pending)? If Pending — `kubectl delete pod mongodb-edge-0 mongodb-hyperdx-0` to unstick the MCK agent. (8) Do all SRPs have the expected `sourceKey` fields? If missing — delete SRP and `helm get manifest byte-secrets-operator | kubectl apply -f -`. (9) Are stuck Helm releases in `uninstalling` state blocking new packs? `helm uninstall --no-hooks`.

Context: This sequence was discovered empirically over a 3h debugging session — each fix exposed the next layer. Following the order avoids backtracking. Most byte-edge cluster recoveries fit this pattern.  _(2026-05-28)_
- Created 3 pack_customization entities in Port for `kfc-uk-lab` deployment group (2026-05-28) by cloning from kfc-uk-lab2: `kfc-uk-lab-byte-secrets-operator-group-customization` (BSO policy cron overrides + MinIO `workloadKind: StatefulSet` — corrected from lab2's Deployment since this cluster runs MinIO as StatefulSet), `kfc-uk-lab-byte-nats-kv-group-customization` (devSeed entries for kfc_uk.4477 + kfc_uk.4477.ocb OAuth clients in EDGE_KV), `kfc-uk-lab-byte-edge-sync-group-customization` (CLOUD_MQTT_BROKER=ssl://mqtt.staging.yumconnect.dev:443). Did NOT clone the scale-down customizations (longhorn defaultReplicaCount=1, minio standalone, nats single-replica) because kfc-uk-lab has 3 nodes vs lab2's smaller footprint.

Context: kfc-uk-lab had zero pack_customizations until 2026-05-28. The devSeed for store 4477 is what populated EDGE_KV with oauth.clients.kfc_uk.4477 — required for edge-api OAuth flows. Future store number changes need to update both customizations.  _(2026-05-28)_
- byte-edge 2-node cluster production rollout plan (decision 2026-05-27):

Two-track approach:
- Track A (test): g5011-v2 and future test stores. Use as drill cluster. Accept data loss for fast iteration on settings.
- Track B (prod): All settings PRE-engineered, backups mandatory, never use salvage as recovery technique.

PRODUCTION HARD RULES:
1. default-replica-count MUST be 2 BEFORE any volume is provisioned on a prod cluster — verify post-greenfield with: kubectl get volume.longhorn.io -n longhorn-system -o custom-columns=NAME:.metadata.name,REPLICAS:.spec.numberOfReplicas (all must show 2)
2. Recurring Longhorn backup job to S3 MANDATORY for prod (RecurringJob CR, cron 0 2 * * *, task: backup, retain: 7 daily + 4 weekly). Even with flaky edge connectivity — retries are acceptable.
3. Monitoring alert on any volume.longhorn.io with robustness != healthy (Prometheus alert via existing OTel pipeline to ClickHouse/HyperDX)
4. NEVER use volume-attachment-recovery-policy: never on a volume with numberOfReplicas: 1 — only safe with >=2 replicas
5. NEVER recreate PVCs as a prod recovery technique. Only acceptable in test.
6. node-monitor-grace-period: 30s for prod (more conservative than 20s for test)

PHASED ROLLOUT:
- Phase 1: g5011 (test) — apply all settings, drill validates RTO <5min with data preserved
- Phase 2: g5011 (test, +1 week) — repeat drill 3x, confirm stability
- Phase 3: One pilot prod store — same settings, 2 weeks monitoring
- Phase 4: Rolling production via Spectro cluster profile + Port spectro_deployment entities

PRE-FLIGHT CHECK SCRIPT (needed to write): Verify before declaring any cluster prod-ready:
- All PVCs have numberOfReplicas: 2
- All volumes have replicas on BOTH nodes (not duplicated on one)
- Recurring backup job exists and last run was successful
- Longhorn settings table matches the required values
- Simulated node disconnect completes recovery within 5 min

Context: Plan decided 2026-05-27 after node-disconnect investigation revealed g5011-v2 cluster had no data redundancy (default-replica-count: 1). Prod cannot tolerate data loss; test cluster used as sandbox to validate. Phased rollout via Spectro cluster profile.  _(2026-05-27)_
- BE-528 OAuth client seeding design decision (2026-05-20): BSO writes oauth-client-<sanitized-id> Secrets directly to a standard namespace `byte-edge-adapters`. No Reflector, no byte-edge-common hub. byte-nats-kv chart creates the namespace and defaults SRP target's kubernetesSecret.namespace to byte-edge-adapters. The namespace field stays overridable per-target as a quiet escape hatch for teams that justify needing their own namespace — undocumented. Adapters consume via plain secretKeyRef (name = oauth-client-<sanitized-id>, key = client_secret). Reloader handles pod restarts on rotation via adapter-side annotation, not restartTargets.

Context: Replaces the line-58 nats kv get pattern in iss-monorepo. Standardization avoids per-adapter namespace negotiation and keeps the byte-nats-kv chart decoupled from consumer enumeration.  _(2026-05-20)_
- edge-sync CLOUD_MQTT broker URL is set per spectro_deployment_group via pack_customization (NOT per-cluster, NOT in the chart). For kfc-uk-lab2 the customization is `kfc-uk-lab2-byte-edge-sync-group-customization` with values_override pointing CLOUD_MQTT_BROKER to ssl://mqtt.staging.yumconnect.dev:443. OTP stays on the edge_cluster property (cloudMqttOtp). This split makes sense: broker is per-environment (staging vs prod), OTP is per-store. Customization blueprint property is `values_override` (snake_case), relation to group is `deployment_group` (not `spectro_deployment_group`).

Context: g5011 unblocked 2026-05-20 after stacking three fixes: (1) format: string on cloudMqttOtp pack variable in byte-edge-sync 0.2.32 (MR 137), (2) CloudMqttOtp field added to EdgeClusterProperties struct in self-service-temporal-workflow-service (MR 4072), (3) pack_customization for CLOUD_MQTT_BROKER on kfc-uk-lab2. After pod restart, edge-sync registered successfully against ssl://mqtt.staging.yumconnect.dev:443 with persistent OTP 51788937.  _(2026-05-20)_
- Edge store provisioning automation design: Port self-service action "Provision edge store" (orgId/storeNumber/env/deploymentGroup) → Temporal ProvisionEdgeStoreWorkflow: (1) MintOTP activity calls Yum GQL createInStoreDeviceRegistrationCode with service JWT from AWS SM /be/global/yum-service-jwt/<orgId>; (2) WriteOTPToAWSSM at /be/<env>/<orgId>/<storeNumber>/edge-sync/cloud-mqtt-otp; (3) UpsertEdgeCluster Port entity; (4) child workflow DeployArtifactPort byte-edge-sync; (5) poll edge-sync /health for registered. Key blocker: Yum SERVICE JWT per orgId — must come from cloud team (Tomy/Hari/Alex). Hard problems: OTP 15-30min TTL vs Spectro pack apply time on greenfield; idempotency (mint+write in same activity to avoid burning OTPs on retry); NATS rebuild needs separate ReprovisionEdgeStore variant.

Context: Proposed 2026-05-19 as the automation follow-on to the manual unblock above. Lives in self-service-temporal-workflow-service repo. Chart-side secrets: block ships independently — automation just writes to the same deterministic AWS SM path.  _(2026-05-19)_
- edge-sync per-store OTP provisioning (manual, today): (1) human with Yum JWT runs GQL createInStoreDeviceRegistrationCode(storeNumber) — orgId from JWT, OTP TTL 15-30min, ask cloud team (Tomy/Hari) for persistent variant for lab stores; (2) write to AWS SM at deterministic path /be/<env>/<orgId>/<storeNumber>/edge-sync/cloud-mqtt-otp; (3) declare in byte-edge-sync pack.yaml under secrets: with awsSecretPath templated via {{ cluster:* }}, keys: [otp] — byte-edge-cli injects External[REDACTED] + upserts pack_[REDACTED]; (4) ESO pulls, Reflector mirrors, deployment uses envFrom secretRef → CLOUD_MQTT_OTP. CLOUD_MQTT_ENABLED default was "false" — Tomy's hotfix flipped to "true" in main 2026-05-19.

Context: Designed 2026-05-19 to unblock g5011 KFC UK store. Uses existing primitives (ESO/BSO/AWS SM/secrets: block) — no new infrastructure. Path is identical to future automation target so manual unblock and automation converge.  _(2026-05-19)_
- Helm MR !125 (fix(charts): hot-swap MongoDB rotation by default in BSO + edge) should be CLOSED, not merged. It would flip zeroDowntime: false on shared-services + hyperdx-mongodb SRPs and disable slot user provisioning in byte-mongodb-edge — undoing the correct ZDR path that BSO actually requires against MCK. The hot-swap path has an unfixable race with MCK SCRAM regeneration (30-60s). z531003's stuck-rotation incident traced back to a partial application of !125's intent (zeroDowntime: null in deployed SRP).

Context: https://gitlab.com/yumbrands/yumdev/byte-edge/helm/-/merge_requests/125. Confirmed 2026-05-13 during live rotation testing on z531003.  _(2026-05-14)_
- BE-563 ticket created (Jira) to consolidate byte-secrets-operator and byte-secrets-operator-crds into a single chart following the ESO pattern (templates/crds/ + crds.create flag + Helm pre-install/pre-upgrade hooks). Eliminates manual CRD sync drift between operator config/crd/bases and helm crds.yaml, removes two-pack publishing surface, frees up a Spectro install-priority slot. Was originally decided 2026-05-06, deferred until BSO v0.11.0 rotation fix work landed; now unblocked.

Context: Ticket: https://yumbrands.atlassian.net/browse/BE-563. Created 2026-05-14 after MR !27 + !131 merged.  _(2026-05-14)_
- BSO v0.11.0 + helm chart 0.10.8 (MR !27 + !131) shipped the rotation incident fix (prune helper, SourceKey API field, SCRAM-SHA-256 verifier pin, drop admin/monitor targets) but missed the paired chart-side dep entry that makes SourceKey actually propagate slot usernames. Followup: BSO MR !28 (e2e fixture adds MONGODB_USERNAME dep entry) + helm MR !132 (chart 0.10.9 adds MONGODB_USERNAME dep entry with sourceKey: username on both MongoDB SRPs). Without !132 every staged rotation breaks consumer auth on next pod cycle because consumers fall back to ConfigMap default byteapp/hyperdx whose passwords are never rotated.

Context: 2026-05-13/14: ran live rotations on z531003 and g6012 after deploying chart 0.10.8 + BSO v0.11.0. shared-services SRP rotation broke edge-api/edge-sync auth on g6012 because chart values had MONGODB_[REDACTED] entry but no MONGODB_USERNAME dep entry. Manual SRP patch + SRR confirmed the fix works end-to-end on both clusters.  _(2026-05-14)_
- BE-533 final design decision: stay on cookie-auth (Option A), drop update goal. Loader: login → create-if-absent by name → POST only, no PATCH/PUT. Why: PATCH on /api/dashboards/:id returns 500 on self-hosted 2.x, and switching to Option B ([REDACTED] /api/v2 with user.accessKey) was rejected to avoid adding mongosh dependency to the loader. Trade-off accepted: fixes to chart-shipped dashboards do NOT propagate to existing clusters; operators must delete the dashboard in UI to force re-create on next chart upgrade.  _(2026-05-08)_
- BE-533 dashboard bootstrap design decision: tag-aware update model. Chart ships JSONs with tags:['byte-edge:managed']; loader matches by name AND tag — PUTs chart-managed ones (chart is source of truth, fixes propagate to existing clusters), skips name-collisions without the tag (operator-owned), POSTs new ones. Why: 'never update' would mean shipping a fix never reaches existing clusters; 'always update' would wipe operator UI work. Tag-aware splits the difference cleanly.  _(2026-05-08)_
- 2026-05-07 z531003-admin: Created Port pack_customization `yum-test-group-byte-secrets-operator-group-customization` setting `policies.sharedServices.mongodb.zeroDowntime: false` as a workaround to stop the cascading edge-api restart loop until chart adds admin/otel-monitor slot users. Hot-swap path persists per-target progress so app-[REDACTED]t be re-rotated when admin/monitor verify timeouts fire. hyperdxMongodb policy intentionally left on staged — same gap exists but isn't crashing anything user-facing.  _(2026-05-07)_
- Design decision 2026-05-07: BSO failure-handling redesign needs three behaviors — (1) per-target state isolation so succeeded targets aren't re-rotated when a sibling fails, (2) consecutive-failure budget that auto-halts the SRP (suspend or status.haltedReason) after N failures to prevent cascading outages, (3) exponential backoff with cap replacing the flat 5-min requeue. Rollback semantics are not needed for current staged failures because VerifyStandby failing means Activate never ran — active slot and dependent secrets still hold the previous-good password. The real bug is that the controller restarts rotation from CreateStandby for ALL targets each cycle, generating a fresh [REDACTED] re-triggers Reloader on already-activated targets. Validated by z531003-admin outage where app-[REDACTED] getting re-rotated for 43h because admin/monitor verify timeouts triggered full restarts.  _(2026-05-07)_
- SESSION HANDOFF 2026-05-06 (continue tomorrow 2026-05-07):

THE BUG: BSO sharedServices SRP fails on z531003-admin (and structurally on every cluster) because zeroDowntime: true treats every target as ping-pongable, but byte-mongodb-edge chart only renders slot users (-a/-b) for appUsers (byteapp, hyperdx) — admin and otel-monitor have no slots. BSO writes to slot b, MongoDB has no admin-b user, verify ping fails after 5min timeout. z531003-admin currently in phase=Error since 2026-05-03 02:00 UTC. g6012 hasn't tripped yet only because its BSO pod restarted 2026-05-04 (after the cron) — next cron 2026-05-10 02:00 UTC will also fail.

OPEN MRs FOR THIS WORK:
- !21 byte-secrets-operator (https://gitlab.com/yumbrands/yumdev/byte-edge/byte-secrets-operator/-/merge_requests/21): Open, prtinter-column type=date→string for Next Rotation. Source change already on main via prior MR; only the .gitlab-ci.yml GOPATH export remains. Helm chart's CRDs already have type=string at 0.10.4 — clusters showing <invalid> just need a Spectro redeploy of byte-secrets-operator-crds 0.10.4, NOT a chart bump.
- !23 byte-secrets-operator (https://gitlab.com/yumbrands/yumdev/byte-edge/byte-secrets-operator/-/merge_requests/23): Open, branch fix/e2e-hot-swap-and-prod-parity. Operator runtime fix (StageHotSwapVerify cache + 30s requeue + namespaces RBAC) + huge prod-parity e2e infra (Go consumer binary, lib.sh helpers, 4 e2e suites, CI shim). Many session-end file reverts may have rolled back parts of e2e infra — verify branch state on resume.
- !125 helm (https://gitlab.com/yumbrands/yumdev/byte-edge/helm/-/merge_requests/125): Open, branch fix/byte-secrets-operator-hot-swap-default. byte-secrets-operator + byte-mongodb-edge zeroDowntime: false default + explicit mongoUsername/authDB on every target. CRDs Chart.yaml bump (0.10.4→0.11.0) is a no-op — should drop it (templates/crds.yaml unchanged).

FOLLOW-UP TASKS:
- Task #17 (pending): Cleanup byte-secrets-operator values.yaml structure — group mongodb policies adjacent, drop redundant targetClass: Infrastructure, header comments. Separate small MR after !23/!125.
- Task #18 (pending): Consolidate byte-secrets-operator-crds into byte-secrets-operator chart following ESO pattern (templates/crds with crds.create flag + helm hooks). Reduces deployed surface. Migration risk: helm.sh/resource-policy: keep on CRDs to prevent cascade deletion. Test on lab cluster first.

CONFIRMED FACTS:
- Pack values byte-identical between z531003-admin and g6012
- BSO image differs: z531003-admin runs v0.10.1; g6012 runs v0.10.1-partial-bootstrap-ff4b564 (CI build of MR !22 dev branch). MR !22 squash-merged into main 2026-05-04 as commit dc3eb40. Our MR !23 inherits MR !22 changes.
- ESO pattern allows single chart to ship CRDs+operator using templates/crds with helm hooks; we currently use separate charts because of Spectro pack model (-13 priority for CRDs, -11 for operator) — can consolidate but accept hook-driven ordering.

NEXT-STEP DECISIONS NEEDED:
1. Drop CRDs Chart.yaml bump from MR !125? (Yes recommended — no-op)
2. Suspend shared-services SRP on z531003-admin pre-deploy to stop further error noise?
3. Land MR !21 standalone (it's tiny + safe)
4. Decision on splitting MR !23: chart-fix portion vs prod-parity infra (separate MR)

LOCAL E2E STATE: minio suite reaches 12 PASS markers, fails at SRR rotation phase (deeper MinIO strategy interaction). Cold-start, migration, nats not run end-to-end yet (runtime kills long background tasks). User will need to run all four locally to validate.  _(2026-05-07)_
- BSO OAuth client seeding design approved 2026-05-06: spec committed at byte-secrets-operator/docs/superpowers/specs/2026-05-06-bso-oauth-client-seeding-design.md on branch feat/oauth-client-bso-strategy. Phase 1 = seeding + drift correction only via new oauthClient strategy; rotation logic (Execute path, multi-version secrets[]) deferred but CRD shape supports it without breaking changes. Replaces byte-nats-kv dev-seed Helm Job with SRP CR.  _(2026-05-07)_
- Decision (2026-05-06): Consolidate byte-secrets-operator + byte-secrets-operator-crds Helm charts into a single chart following the ESO (External Secrets Operator) pattern — templates/crds/ with crds.create flag + helm hooks for ordering. Driver: reduce deployment surface (one Spectro pack instead of two), eliminate manual CRD sync drift between operator repo and helm chart, single-source-of-truth for CRD schema. Trade-off accepted: lose explicit Spectro install-priority ordering, rely on Helm hook ordering instead. Out of scope for current MRs !23/!125; track as follow-up after they land.  _(2026-05-07)_
- BSO OAuth client management design scoped to phase 1 = seeding only (replace byte-nats-kv devSeed Helm Job). New `oauthClient` strategy implementing BootstrapStrategy + Verify for drift detection. Hybrid generate/external mode per target. Rotation logic (Execute, multi-version secrets[], time-based) explicitly out of scope for v1; CRD shape designed to support it later without breaking changes. Helm chart renders SRP CR instead of Job — pack_customization remains source-of-truth for entries.  _(2026-05-06)_
- kfc-uk-lab2 (g6012 cluster) deployment_group has three pack_customizations applied 2026-05-06 to fit Win11 VM on 2-node 256GB cluster: byte-nats-jetstream (cluster.enabled:false, replicas:1, no PDB, no topologySpreadConstraints), byte-minio (mode:standalone, replicas:1, drivesPerNode:1, persistence size:10Gi), byte-longhorn (defaultReplicaCount:1, defaultClassReplicaCount:1; reserved-percentage kept at prod-safe 30%). Production groups untouched.  _(2026-05-06)_
- For per-cluster scoped values that map to a single cluster property (like orgId), use Spectro pack variable with defaultValue: "{{ cluster:propertyName }}" referenced as {{.spectro.var.X}} in values.spectro.yaml — NOT pack_customization. Why: pack_customization is for sensitive/multi-key/complex overrides; for a single string with one source of truth (the edge_cluster Port entity), the pack-variable + cluster-property pattern is cleaner and matches how byte-edge-common already does {{ cluster:awsAccessKey }}. How to apply: when a value differs per cluster but is a property of the cluster itself, add it to edge_cluster blueprint, declare it in pack.yaml variables[] with cluster: defaultValue, reference via {{.spectro.var.X}} in values.spectro.yaml. Decided 2026-05-06 for byte-edge-api/sync ORG_ID fix.  _(2026-05-06)_
- Migrated pack_[REDACTED] direct reference to relational discovery via SearchEntitiesByRelation. pack_[REDACTED] created at publish-time, not pre-declared. Enables dynamic [REDACTED] on deploy without manual Port setup.  _(2026-05-01)_
- For BE-496 chart auto-publish on byte-edge-core, the chosen design is: rewrite `publish:bump-version` to (1) fetch pipeline-templates/ci-scripts via git clone, (2) call publish-pack.sh per chart instead of inlining byte-edge-cli pack publish (reuses battle-tested script with add-helm-repos.sh + helm dep update + env validation), (3) keep [skip ci] semantics on the bump commit, (4) add `git pull --rebase origin main` before the final push to handle the widened race window from inline publish. Risks accepted: 409-on-retry wedge if publish partially succeeds (rare, manual recovery via package delete + retry); operator must monitor.  _(2026-04-30)_
- For BE-496, the ACTUAL proper fix is collapsing the two-pipeline split (image at SHA N, chart at SHA N+1 via autobump) into one atomic parent pipeline using GitLab `trigger:include` with `strategy: depend`. The dynamic chart child pipeline (generate-child-pipeline + trigger-child-pipeline) already runs on the same SHA as the parent — we just need to gate it on `*docker_changes` (cutting a release) rather than `charts/**/*` (which only fires on the autobump commit, requiring [skip ci] to be dropped). Image+chart ship atomically or fail together. [skip ci] keeps original meaning on the bump commit.  _(2026-04-29)_
- For BE-496 chart-publish, the proper fix is removing VERSION from .code_change_paths AND .docker_change_paths AND dropping [skip ci] from the autobump commit. Why: VERSION was the loop source — it's only ever bumped by publish:bump-version itself, but it was listed as a build-trigger path, which forced [skip ci] on every autobump to break the loop. [skip ci] then suppressed the chart-publish dynamic pipeline that was supposed to fire on the chart bumps. Removing VERSION from change paths eliminates the contradiction at its source: the autobump commit naturally matches NOTHING in the parent pipeline (only `charts/**/*` for the chart child pipeline), so no skips/regex hacks needed. Reuses the existing dynamic chart pipeline (full-pipeline → generate-child-pipeline → discovers changed charts via HEAD~1..HEAD diff) untouched.  _(2026-04-29)_
- For BE-496 chart-publish, settled on Approach C (inline publish in publish:bump-version using upstream pipeline-templates/ci-scripts/publish-pack.sh — no script duplication). Rejected: A1/A2 (title-based job skips conflate [skip ci] semantics, brittle); B1 (API trigger still requires guards on docker/bump because the new pipeline matches their rules anyway). C wins because: loop-safe by construction (single job, no separate triggering pipeline), [skip ci] keeps "skip all" meaning, reuses proven publish-pack.sh + add-helm-repos.sh, single transactional sequence (publish before commit). Wart: 409 on retry of partial-failure requires manual recovery — accepted as rare and documented.  _(2026-04-29)_
- For BE-496 chart-publish fix, chose approach A (drop [skip ci], gate via $CI_COMMIT_TITLE =~ /^chore\(release\)/) over inline-publish. Reason: A reuses the proven chart-pipeline (publish-pack.sh + add-helm-repos.sh), gives free GitLab retries, and avoids duplicating publish logic in a second script that drifts. Inline-publish bypasses the chart pipeline's lint/scan/discover stages and removes the manual-gate UX that publish-single.yml relied on.  _(2026-04-29)_
- Migration plan saved at docs/superpowers/plans/2026-04-29-kfcuk-byte-edge-infra-migration.md to retire kfc-uk-lab-kfc-us-edge-setup-1.0.13 and consolidate onto kfc-uk-lab-byte-edge-infra. 4 phases: prep overlays + new profile version, same-pack adoption (spectro-proxy/k8s-dashboard/lb-metallb-helm), same-ns rename (reflector/ESO), cross-ns swap (longhorn + traefik). Two unresolved decisions before execution: byte-metallb vs lb-metallb-helm canonical choice, and traefik namespace strategy (move to traefik-system vs keep in default). Not started — execution deferred.  _(2026-04-29)_
- Decision (2026-04-29): For kfc-uk-lab-byte-edge-infra profile, swap byte-metallb (Yum-wrapped Helm chart) for lb-metallb-helm (Spectro public pack with valuesOverlay setting priority -45 and IP pool config). Reason: prefer Spectro upstream pack with overlay overrides over maintaining a Yum wrap when the overlay path now exists. byte-metallb-config is therefore NOT needed since lb-metallb-helm bakes the IPAddressPool into the same pack via overlay.  _(2026-04-29)_
- Migration decision (2026-04-28): kfc-uk-lab-kfc-us-edge-setup profile (6 packs: lb-metallb-helm/longhorn/spectro-k8s-dashboard/reflector/spectro-proxy/traefik) is being consolidated into byte-edge-infra. The 4 OSS packs are already replaced by byte-* equivalents in byte-edge-infra. Both spectro-proxy and spectro-k8s-dashboard move to pack_group: byte-edge-infra in helm/external-charts-config.yaml — user explicitly chose both in infra (not observability split). Why: spectro-proxy is required for edge-native cluster reachback; user wants single canonical infra profile rather than split.  _(2026-04-28)_
- byte-nats-kv devSeed Job (MR !113): seeds NATS EDGE_KV OAuth clients on lab clusters via pack_customization (enabled on yum-test-group, kfc-uk-lab, kfc-uk-lab2 only). bcrypt hash of dev-[REDACTED] in template. Image: registry.gitlab.com/.../container-images/nats-box:0.14.3 (mirrored).  _(2026-04-28)_
- BSO design decision 2026-04-24: Bootstrap of dependent secrets tolerates partial success (missing-dep classified, requeued via Namespace watches); Rollback is all-or-nothing and returns aggregated error on ANY dep failure — rationale: partial rollback produces split-brain credential state where some consumers have old [REDACTED] some new, neither version universally valid, strictly worse than no rollback.  _(2026-04-24)_
- BE-497: Add Spectro native pack source type to byte-edge-cli registry sync — extends external-charts-config.yaml with a new `type: spectro` source that queries Spectro API (SearchPacks/GetPackVersions) and upserts pack + pack_version entities in Port only (no GitLab artifact sync). Motivation: Spectro-managed packs like Virtual Machine Orchestrator (VMO) are OCI-only with no Helm HTTP index, so they can't be synced via the existing helm source type. Linked to epic BE-111.  _(2026-04-23)_
- Memory posture decision (2026-04-22): Memento MCP is primary write target for all session-derived memories (learnings, decisions, preferences, root causes, outcomes); native Claude MEMORY.md index is auto-loaded read-only and curated manually, with stable Memento memories promoted to native topic files only on explicit request. Rationale: Memento's graph ranking + existing hook investment beats native file recall, but keeping native MEMORY.md as a deterministic session-start index costs nothing and provides a curated read-lane.  _(2026-04-22)_
- [REDACTED] stack is production-ready for Store 1 without AWS IoT Core. BSO + AWS SM + ESO + byte-edge-common is the complete working solution live on KFC UK lab. IoT Core (fleet tracking + certificate lifecycle) is a post-Store 1 enhancement scoped by Michael. The ESO+SM fallback path is not a fallback — it IS the plan.  _(2026-04-22)_
- Harbor is unblocked work with no architecture alignment dependency — just needs a Jira ticket and a sprint. Production Harbor instance goes in shared-services account as a pull-through proxy for Docker Hub, quay.io, registry.k8s.io. GitLab Dependency Proxy is the current stopgap but won't survive fleet scale (~5+ clusters is the risk threshold).  _(2026-04-22)_
- Temporal cloud-to-site connectivity: deployment workflows (Spectro + Port.io) don't need cloud-to-site today — both are internet-accessible. ZTP workflows that touch edge hardware DO need it. Mitigation for Store 1: run from VPN-connected laptop. Cloud-to-site (VPN tunnel or Tailscale) is a Q3 2026 target; requires Networking + GRE + KFC UK IT alignment on which solution is acceptable in Yum's network policy.  _(2026-04-22)_
- Store 1 observability plan: local per-cluster HyperDX is the solution for KFC UK Store 1 — fully operational on KFC UK lab. Access for GRE/KFC UK staff via VPN + port-forward or BSO-provisioned local accounts. Centralized telemetry pipeline is post-Store 1 work requiring GRE Incident Management involvement and a data flow architecture decision first.  _(2026-04-22)_
- GRE architectural position on gre-graph: byte-edge-dgs belongs in gre-graph (ADR 202), not as a standalone API gateway. The only open question is infrastructure tenancy — which AWS account and region hosts gre-graph. GRE proposal: single shared-services account, US East initially. This is the only genuine Store 1 blocker in the cloud architecture alignment.  _(2026-04-22)_
- Port.io is already functioning as the operational CMDB for Byte Edge — edge_cluster, pack_version, spectro_deployment, spectro_deployment_group, pack_customization entities are all live. Architecture team is unaware of this; needs to be explicitly communicated in the alignment meeting to prevent a parallel CMDB investment.  _(2026-04-22)_
- pack_[REDACTED] should relate to `pack` (not `pack_version`) — entity ID format `{pack_name}-{secret_name}` — stable across version bumps, avoids entity explosion at scale (N_packs × K_versions × J_keys). Migration: delete all old versioned pack_[REDACTED] and republish.  _(2026-04-21)_
- User uses port + gre-graph MCP servers regularly from outside the byte-edge workspace (other Yum repos, scripts, ad-hoc directories), so these MCPs must stay in global ~/.claude.json `mcpServers` — not project-scoped via .mcp.json. Why: the [REDACTED] of project-local scoping (~200 tokens/session in non-byte-edge sessions) is marginal once Claude Code's deferred-tool-loading kicks in, and not worth the "is port available here?" friction when using them elsewhere. How to apply: for future MCP-scope decisions about company-specific servers, default to global unless there's a real secret-leak risk or the server only functions in one project.  _(2026-04-21)_
- Cost-optimized secrets architecture implemented in byte-edge-cli (branch feat/pack-list-get, image tag secrets_poc): SecretDeclaration now has Keys []string field; InjectExternalSecrets generates ExternalSecrets with data[].remoteRef.property pattern (key selection) instead of dataFrom.extract (full blob); multiple K8s Secrets can reference the same AWS SM [REDACTED] while extracting different keys. This enables 10x AWS SM cost reduction ($0.40/secret/month) while maintaining Reloader isolation. iss-monorepo MR !142 updated to use single /kfc-uk/{{ cluster:$title }}/iss-secrets path with keys arrays for iss-adapter-creds, iss-rmf-creds, iss-qsr-creds.  _(2026-04-21)_
- BSO namespace policy: EVERY byte-edge cluster must use the SAME BSO namespace (`byte-secrets-operator`). The old `byte-secrets-operator-system` namespace is deprecated. yum-test-group still on old ns (0.10.6) pending migration. Mixed fleet = ops pain (different kubectl commands per cluster).  _(2026-04-17)_
- Architectural decision: all clusters must use the SAME namespace for BSO → `byte-secrets-operator` (the new namespace from MR !101). yum-test-group currently on `byte-secrets-operator-system` must be migrated. BSO chart pack.namespace is frozen at `byte-secrets-operator` going forward; `0.10.6` (published with old `-system` namespace) becomes deprecated once yum-test-group migrates. One BSO namespace across the fleet = one publish track, no more parallel "per-cluster golden" versions.  _(2026-04-16)_
- claude-switch cost optimization tweaks (~/scripts/claude-switcher/bin/claude-switch): (1) Default model_tier from "opus" to "sonnet" line 460 — Sonnet is 60% of Opus pricing across every [REDACTED], save Opus for explicit --opus flag. (2) Add --opusplan tier mapping to "opusplan" alias — uses Opus only during Plan Mode (Shift+Tab), Sonnet for execution, matches detailed-plans-then-execution workflow. (3) Lower max_output_tokens_bedrock_default and max_output_tokens_anthropic_default from 16384 to 8192 lines 442-443 — output is $25/M on Opus, 8K covers 99% of turns. (4) Add ENABLE_TOOL_SEARCH=auto:0 to both apply_bedrock and apply_anthropic to force lazy MCP tool loading even under the 10% threshold. (5) Add --haiku tier for research/read-heavy sessions — Haiku is 5x cheaper than Opus, alias cbh. (6) Lower thinking_default from 2048 to 1024 line 446 — extended thinking bills as output, --think still gives 4K when needed. (7) Keep autocompact_pct_default=95 and subagent_model_bedrock_default=haiku as-is — both already optimal.  _(2026-04-11)_
- byte-edge-core needs consolidation into a single Helm chart — current architecture has separate byte-edge-api and byte-edge-sync charts, but Temporal deployment workflow may be importing the whole profile instead of updating individual packs, causing version drift issues.  _(2026-04-09)_
